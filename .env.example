# URL de l'API backend en production (laisser vide en dev : le proxy Vite s'en charge).
# Cloudflare (Worker) : https://api.2fgeneral.fr  → à définir dans les variables
# d'environnement du projet Cloudflare Pages (build).
VITE_API_URL=
